            <div id="tab-financials" class="tab-content hidden space-y-6">
                <!-- Financial Cards -->
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                    <div class="bg-emerald-50 dark:bg-emerald-950/20 border border-emerald-100 dark:border-emerald-900/30 p-5 rounded-2xl">
                        <p class="text-xs font-bold text-emerald-800 dark:text-emerald-400 uppercase">Net Profit Margin</p>
                        <h4 id="fin-net-profit" class="outfit-font text-2xl font-black text-emerald-700 dark:text-white">0.00 L.E.</h4>
                        <p id="fin-net-margin" class="text-xs text-slate-400 mt-1">Margin: 0%</p>
                    </div>

                    <div class="bg-white dark:bg-darkCard p-5 border rounded-2xl shadow-sm">
                        <p class="text-xs font-bold text-slate-400 uppercase">Total Revenue Generated</p>
                        <h4 id="fin-revenue" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 L.E.</h4>
                    </div>

                    <div class="bg-white dark:bg-darkCard p-5 border rounded-2xl shadow-sm">
                        <p class="text-xs font-bold text-slate-400 uppercase">Total Operating Expenses</p>
                        <h4 id="fin-expenses" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 L.E.</h4>
                    </div>

                    <div class="bg-white dark:bg-darkCard p-5 border rounded-2xl shadow-sm">
                        <p class="text-xs font-bold text-slate-400 uppercase">Taxes & VAT Ledger</p>
                        <h4 id="fin-taxes" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 L.E.</h4>
                    </div>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    <!-- Expenses Breakdown -->
                    <!-- Financial Expenses Breakdown Ledger -->
                    <div class="bg-white dark:bg-darkCard p-6 border rounded-2xl shadow-sm space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">Operating Expenses Breakdown</h3>
                        <div class="overflow-x-auto">
                            <table id="table-fin-expenses" class="w-full text-left border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase text-[9px] tracking-wider">
                                        <th class="py-2.5">Expense Category</th>
                                        <th class="py-2.5 text-right">Value (L.E.)</th>
                                        <th class="py-2.5 text-right">Share %</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Populated dynamically in JS -->
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- Payment reconciliation -->
                    <div class="bg-white dark:bg-darkCard p-6 border rounded-2xl shadow-sm lg:col-span-2 space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">Cash Drawer Payment Reconciliations (Real-time Audit)</h3>
                        <div class="overflow-x-auto">
                            <table id="table-fin-reconciliation" class="w-full text-left border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                        <th class="py-2.5">Payment Method</th>
                                        <th class="py-2.5">System Orders</th>
                                        <th class="py-2.5">System Revenue</th>
                                        <th class="py-2.5">Physical Audit Count</th>
                                        <th class="py-2.5">Physical Audit Value</th>
                                        <th class="py-2.5">Discrepancy (Variance)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Filled dynamically -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- Daily Shift Closing Audit Logs -->
                <div class="bg-white dark:bg-darkCard p-6 border rounded-2xl shadow-sm space-y-4">
                    <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">Daily & Shift Closing Accounts Audit Ledger</h3>
                    <div class="overflow-x-auto">
                        <table id="table-fin-closings" class="w-full text-left border-collapse text-xs">
                            <thead>
                                <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                    <th class="py-2.5">Date</th>
                                    <th class="py-2.5">Audited By</th>
                                    <th class="py-2.5">Expected Cash Drawer</th>
                                    <th class="py-2.5">Actual Cash Collected</th>
                                    <th class="py-2.5">Cash Discrepancy</th>
                                    <th class="py-2.5">Card Revenue</th>
                                    <th class="py-2.5">Online Revenue</th>
                                    <th class="py-2.5">Total Revenue Closed</th>
                                    <th class="py-2.5">Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Filled dynamically -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
