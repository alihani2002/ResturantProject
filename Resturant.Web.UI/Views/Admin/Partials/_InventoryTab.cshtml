            <div id="tab-inventory" class="tab-content hidden space-y-6" dir="rtl">
                <!-- Inventory Metrics -->
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6 text-right">
                    <div class="bg-white dark:bg-darkCard p-5 border border-slate-200 dark:border-darkBorder rounded-2xl flex items-center justify-between">
                        <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">قيمة المخزون</p>
                            <h4 id="inv-valuation" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 ج.م</h4>
                        </div>
                        <div class="p-3 bg-teal-50 dark:bg-teal-950/20 text-teal-500 rounded-lg"><i class="bi bi-wallet2 text-xl"></i></div>
                    </div>
                    <div class="bg-white dark:bg-darkCard p-5 border border-slate-200 dark:border-darkBorder rounded-2xl flex items-center justify-between">
                        <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">المشتريات والتوريدات</p>
                            <h4 id="inv-purchases" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 ج.م</h4>
                        </div>
                        <div class="p-3 bg-blue-50 dark:bg-blue-950/20 text-blue-500 rounded-lg"><i class="bi bi-cart-check text-xl"></i></div>
                    </div>
                    <div class="bg-white dark:bg-darkCard p-5 border border-slate-200 dark:border-darkBorder rounded-2xl flex items-center justify-between">
                        <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">الهالك والتالف في المطبخ</p>
                            <h4 id="inv-waste" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0.00 ج.م</h4>
                        </div>
                        <div class="p-3 bg-red-50 dark:bg-red-950/20 text-red-500 rounded-lg"><i class="bi bi-trash3 text-xl"></i></div>
                    </div>
                    <div class="bg-white dark:bg-darkCard p-5 border border-slate-200 dark:border-darkBorder rounded-2xl flex items-center justify-between">
                        <div>
                            <p class="text-xs font-bold text-slate-400 uppercase">نسبة تكلفة الأغذية</p>
                            <h4 id="inv-foodcost" class="outfit-font text-2xl font-black text-slate-800 dark:text-white">0%</h4>
                        </div>
                        <div class="p-3 bg-amber-50 dark:bg-amber-950/20 text-amber-500 rounded-lg"><i class="bi bi-percent text-xl"></i></div>
                    </div>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 text-right">
                    <!-- Low Stock Alerts -->
                    <div class="bg-white dark:bg-darkCard p-6 border border-slate-200 dark:border-darkBorder rounded-2xl shadow-sm lg:col-span-2 space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-red-500 flex items-center gap-2">
                            <span class="w-2 h-2 rounded-full bg-red-500 animate-ping"></span> تحذيرات نقص المخزون وطلبات التوريد
                        </h3>
                        <div class="overflow-x-auto">
                            <table id="table-inv-lowstock" class="w-full text-right border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                        <th class="py-2.5 text-right">اسم الخامة</th>
                                        <th class="py-2.5 text-center">المخزون الحالي</th>
                                        <th class="py-2.5 text-center">حد إعادة الطلب</th>
                                        <th class="py-2.5 text-center">الوحدة</th>
                                        <th class="py-2.5 text-right">اسم المورد</th>
                                        <th class="py-2.5 text-right">حالة التنبيه</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Filled dynamically -->
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- Supplier Performance -->
                    <div class="bg-white dark:bg-darkCard p-6 border border-slate-200 dark:border-darkBorder rounded-2xl shadow-sm space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">تدقيق مشتريات الموردين</h3>
                        <div class="overflow-x-auto">
                            <table id="table-inv-suppliers" class="w-full text-right border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                        <th class="py-2.5 text-right">اسم المورد</th>
                                        <th class="py-2.5 text-center">عدد المشتريات</th>
                                        <th class="py-2.5 text-left">إجمالي القيمة</th>
                                        <th class="py-2.5 text-left">مدة التوريد</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Filled dynamically -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 text-right">
                    <!-- Food Cost Recipe Margins -->
                    <div class="bg-white dark:bg-darkCard p-6 border border-slate-200 dark:border-darkBorder rounded-2xl shadow-sm space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">تحليل هامش ربح الوصفات والمكونات</h3>
                        <div class="overflow-x-auto">
                            <table id="table-inv-recipes" class="w-full text-right border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                        <th class="py-2.5 text-right">المنتج (الصنف)</th>
                                        <th class="py-2.5 text-left">سعر البيع</th>
                                        <th class="py-2.5 text-left">تكلفة المكونات</th>
                                        <th class="py-2.5 text-left">الهامش الإجمالي</th>
                                        <th class="py-2.5 text-left">نسبة تكلفة الأغذية %</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Filled dynamically -->
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- Waste Log report -->
                    <div class="bg-white dark:bg-darkCard p-6 border border-slate-200 dark:border-darkBorder rounded-2xl shadow-sm space-y-4">
                        <h3 class="outfit-font text-sm font-bold text-slate-900 dark:text-white">سجلات تالف وهالك المطبخ</h3>
                        <div class="overflow-x-auto">
                            <table id="table-inv-waste" class="w-full text-right border-collapse text-xs">
                                <thead>
                                    <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                        <th class="py-2.5 text-right">المادة الخام</th>
                                        <th class="py-2.5 text-center">الكمية الهالكة</th>
                                        <th class="py-2.5 text-left">الأثر المالي</th>
                                        <th class="py-2.5 text-right">سبب الهلاك</th>
                                        <th class="py-2.5 text-right">تاريخ التسجيل</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Filled dynamically -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
