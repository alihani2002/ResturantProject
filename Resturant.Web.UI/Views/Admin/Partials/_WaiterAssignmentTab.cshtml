            <div id="tab-waiter-assignment" class="tab-content hidden space-y-6">
                <div class="bg-white dark:bg-darkCard p-6 border border-slate-200 dark:border-darkBorder rounded-2xl shadow-sm space-y-4">
                    <h3 class="outfit-font text-base font-bold text-slate-800 dark:text-white flex items-center gap-2">
                        <i class="bi bi-person-lines-fill text-indigo-500"></i> Assign Waiters to Tables
                    </h3>
                    <p class="text-sm text-slate-500 dark:text-slate-400">Select a waiter for each table. Waiters will only see and receive notifications for the tables assigned to them.</p>
                    <div class="overflow-x-auto mt-4">
                        <table id="table-waiter-assignment-list" class="w-full text-left border-collapse text-xs">
                            <thead>
                                <tr class="border-b border-slate-100 dark:border-darkBorder text-slate-400 font-bold uppercase">
                                    <th class="py-3">Table Number</th>
                                    <th class="py-3">Assigned Waiter</th>
                                    <th class="py-3 text-right">Actions</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-slate-100 dark:divide-darkBorder">
                                <!-- Dynamic rows -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
