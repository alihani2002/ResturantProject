<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - نظام إدارة المطعم</title>
    <!-- Google Fonts Cairo -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.rtl.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Resturant.Web.UI.styles.css" asp-append-version="true" />
    <link rel="icon" type="image/png" href="~/assets/images/q.png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body, .navbar, .footer-custom, button, input, select, textarea {
            font-family: 'Cairo', 'Segoe UI', Tahoma, Geneva, sans-serif !important;
        }
    </style>
</head>
<body>
    @if (ViewData["Title"]?.ToString() != "Qore Menu")
    {
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm mb-4 py-3">
            <div class="container d-flex flex-column align-items-center">
                <!-- Brand Centered -->
                <div class="navbar-brand-centered">
                    <a class="navbar-brand d-flex align-items-center gap-2" asp-area="" asp-controller="Home" asp-action="Index">
                        <img src="~/assets/images/Qore.png" alt="QoreSystem" class="me-2" style="height: 32px; width: auto;" onerror="this.src='~/assets/images/logo-abbr.png'" />
                        <span class="text-black-pure fw-bold fs-2" style="letter-spacing: 1px;">QoreSystem</span> <!-- Brand -->
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="تبديل التنقل">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Links Centered Below Brand -->
                <div class="navbar-collapse collapse w-100 justify-content-center">
                    <ul class="navbar-nav mb-2 mb-lg-0 gap-4">
                        <li class="nav-item">
                            <a class="nav-link text-black-pure fw-bold text-uppercase" asp-area="" asp-controller="Home" asp-action="Index">الرئيسية</a>
                        </li>
                    </ul>
                    <!-- Login Partial (Absolute right/left on desktop based on direction) -->
                    <div class="d-lg-flex position-lg-absolute start-0 top-50 translate-middle-y-lg mt-3 mt-lg-0"> 
                         <partial name="_LoginPartial" />
                    </div>
                </div>
            </div>
        </nav>
    </header>
    }
    <main role="main" class="pb-0">
        @RenderBody()
    </main>
    
    <partial name="_OrderHistoryPartial" />

    @if (ViewData["Title"]?.ToString() != "Qore Menu")
    {
    <footer class="footer-custom mt-5 border-top" id="contact">
        <div class="container">
            <div class="row">
            </div>
            <div class="footer-bottom border-top pt-4 mt-4 text-center">
                <p class="text-muted">&copy; @DateTime.Now.Year نظام QORE || مشغل بواسطة CORVENT . جميع الحقوق محفوظة.</p>
            </div>
        </div>
    </footer>
    }
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
